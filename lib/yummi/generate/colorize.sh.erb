#!/bin/sh

# .:: Generated by Yummi ::.

usage() {
  echo "Usage: $0 COLOR TEXT [FORMAT]"

  exit 1
}

<% Yummi::Color::COLORS.each do |key, pattern| %>
function color_<%= key %> {
  echo "<%= "$1".colorize(key) %>"
}
<% end %>

[ "$#" = "0" ] && usage

format="%s"

[ ! -z "$3" ] && format="$3"

printf "$format" $(color_"$@")
